import 'package:flutter/material.dart';

class EmployeeFilterView extends StatelessWidget {
  const EmployeeFilterView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Employee filter'),);
  }
}
